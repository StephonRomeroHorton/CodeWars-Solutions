/* Question ~ Reversed Strings: Complete the solution so that it reverses the string passed into it.

Exanples:

'world'  =>  'dlrow'
'word'   =>  'drow'

Answer:
*/

const solution = str =>  str.split('').reverse().join('');